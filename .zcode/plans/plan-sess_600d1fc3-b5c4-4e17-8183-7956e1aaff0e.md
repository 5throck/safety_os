## Plan: Add Optional base-map MCP Guidance to project-review Skill

### Context
Follow the pattern from `abap_vibe_coding` where base-map MCP is used as an optional, preferred tool with explicit fallback — never hard-dependent. Since base-map MCP is not registered in the project's `.mcp.json` and may not be available to all users, all references must be conditional.

### Files to Modify (3 files)

#### 1. `.agents/skills/project-review/SKILL.md`
- Bump `version` from `1.1.0` → `1.2.0`
- Bump `last_reviewed` to `2026-07-10`
- Add `metadata.tool_guidance` section referencing base-map MCP tools with fallback

#### 2. `.claude/commands/project-review.md`
- Add a **"Tool Guidance (Optional)"** section after Step 2 (before agent dispatch)
- In Step 3 (Dispatch Agents), add a parallel base-map code review call:
  - If `mcp__base-map__review_code` is available, run it on key scripts (`scripts/dev-sync.ts`, `scripts/safety-audit.ts`, etc.) alongside agent dispatch
  - If not available, skip silently — agents already cover code review
- In Step 5 (Action Items), add note that remediation plans should specify `mcp__base-map__implement_code` as preferred execution method with `direct Edit/Write` as fallback

#### 3. `.gemini/commands/project-review.md`
- Mirror the same changes from the Claude command file, adapted for Gemini CLI context
- Same Tool Guidance section, same conditional language

### Pattern for Conditional Usage (consistent with abap_vibe_coding)

```
> **Tool Guidance (Optional):** If base-map MCP is available (`mcp__base-map__*` tools present),
> use `mcp__base-map__review_code` for parallel code review of key scripts.
> If not available, skip — specialist agents already provide domain coverage.
```

For remediation plans output:
```
> **Execution method:** If available, use `mcp__base-map__implement_code` for code generation;
> otherwise, use direct file Edit/Write for all changes.
```

### What This Does NOT Change
- No changes to `.mcp.json` (base-map is external/user-level MCP)
- No changes to `.claude/settings.local.json` or `.gemini/settings.json` (tool permissions)
- No new agent definitions or skill definitions
- The existing 5-step review flow remains identical — base-map is an additive enhancement, not a replacement